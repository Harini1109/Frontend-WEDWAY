<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".DetailsActivity">

    <!-- Logo -->
    <ImageView
        android:id="@+id/logoImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/ring"
        android:contentDescription="App Logo"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bride's Name -->
    <EditText
        android:id="@+id/etBrideName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Bride's Name"
        android:padding="12dp"
        android:background="@android:drawable/editbox_background_normal"
        android:textColor="#000000"
        android:textColorHint="#666666"
        android:layout_marginTop="40dp"
        android:layout_marginHorizontal="32dp"
        app:layout_constraintTop_toBottomOf="@id/logoImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Groom's Name -->
    <EditText
        android:id="@+id/etGroomName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Groom's Name"
        android:padding="12dp"
        android:background="@android:drawable/editbox_background_normal"
        android:textColor="#000000"
        android:textColorHint="#666666"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/etBrideName"
        app:layout_constraintStart_toStartOf="@id/etBrideName"
        app:layout_constraintEnd_toEndOf="@id/etBrideName" />

    <!-- Wedding Date -->
    <EditText
        android:id="@+id/etWeddingDate"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Wedding Date"
        android:padding="12dp"
        android:background="@android:drawable/editbox_background_normal"
        android:textColor="#000000"
        android:textColorHint="#666666"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/etGroomName"
        app:layout_constraintStart_toStartOf="@id/etGroomName"
        app:layout_constraintEnd_toEndOf="@id/etGroomName" />

    <!-- Wedding Time -->
    <EditText
        android:id="@+id/etWeddingTime"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Wedding Time"
        android:padding="12dp"
        android:background="@android:drawable/editbox_background_normal"
        android:textColor="#000000"
        android:textColorHint="#666666"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/etWeddingDate"
        app:layout_constraintStart_toStartOf="@id/etWeddingDate"
        app:layout_constraintEnd_toEndOf="@id/etWeddingDate" />

    <!-- Next Button -->
    <Button
        android:id="@+id/btnNext"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Next"
        android:textColor="@android:color/white"
        android:backgroundTint="#FFC107"
        android:padding="12dp"
        android:layout_marginTop="40dp"
        android:layout_marginHorizontal="64dp"
        app:layout_constraintTop_toBottomOf="@id/etWeddingTime"
        app:layout_constraintStart_toStartOf="@id/etWeddingTime"
        app:layout_constraintEnd_toEndOf="@id/etWeddingTime" />

</androidx.constraintlayout.widget.ConstraintLayout>
